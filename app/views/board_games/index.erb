<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Your Board Games</title>
    <style>
      body {font-family: Arial, Helvetica, sans-serif;}

      button.logout {
        background-color: #f44336;
        color: white;
        padding: 14px 20px;
        margin: 8px 0;
        border: none;
        cursor: pointer;
        float: right;
      }

      button.new-game {
        border: none;
        cursor: pointer;
        background-color: #0D4F8B;
        color: white;
        padding: 15px;
      }

      button:hover {
        opacity: 0.8;
      }


    </style>
  </head>
  <header>
    <form action="/logout" method="get">
      <button type="submit" name="logout" class="logout">Log Out</button>
    </form>
  </header>
  <body>
    <h1>Welcome <%= current_user.username %></h1>

    <% if @user_games.empty? %>
        <p>You do not have any games in your collection, click below to add one.</p>
    <% end %>
    <ol>
      <% @user_games.each do |game| %>
         <li><strong><a href="/games/<%= game.id %>"><%= game.name %></a></strong> <br>
         <%= game.game_type %></li>
         <br>

       <% end %>
    </ol>

    <!-- <a href="/games/new"><h4>Add New Game</h4></a> -->
    <form action="/games/new" method="get">
      <button type="submit" name="new_game" class="new-game">Add New Game</button>
    </form>
  </body>
</html>
